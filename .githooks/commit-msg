#!/usr/bin/env bash
set -euo pipefail

msg_file="${1:-}"
if [[ -z "$msg_file" || ! -f "$msg_file" ]]; then
  exit 0
fi

if grep -qiE '^co-authored-by:' "$msg_file"; then
  echo "ERROR: 'Co-authored-by:' trailers are not allowed in this repository."
  exit 1
fi

